<html>
  <head>
  </head>
  <body>
    <div><%= flash[:branch] %></div>
    <p/>
  <% @branches.each do |branch| %>
    <%= branch.name %>&nbsp;&nbsp;<a href="<%= edit_branch_path(branch) %>">Edit</a><br/>
    &nbsp;&nbsp;<a href="<%= branch_participants_path(branch) %>">Participants</a>&nbsp;&nbsp;<a href="<%= new_branch_event_path(branch) %>">New Event</a>&nbsp;&nbsp;<a href="<%= new_branch_participant_path(branch) %>">New Participant</a>
  <% end %>
  <p/>
  <a href="<%= new_branch_path %>">New Branch</a>
  </body>
</html>
