<h1> Create Participant</h1>
    <form method="post" action="<%= branch_participants_path(@branch) %>">
      <%= render "form" %>
    </form>
    <p/>
    <a href="<%= branch_participants_path(@branch) %>">Return To Participant List</a>
